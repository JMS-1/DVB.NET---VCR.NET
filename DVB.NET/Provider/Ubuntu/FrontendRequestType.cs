// Mögliche Befehl des Clients.
public enum FrontendRequestType
{
    add_section_filter = 0,
    add_stream_filter = 1,
    connect_adapter = 2,
    del_all_filters = 3,
    del_filter = 4,
    tune = 5,
};