namespace JMS.DVB.Provider.Ubuntu
{
    public enum FrontendRequestType
    {
        add_filter = 0,
        connect_adapter = 1,
        del_all_filters = 2,
        del_filter = 3,
        tune = 4,
    }
}